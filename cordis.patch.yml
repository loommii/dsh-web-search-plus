# @loommii/dsh-web-search-plus bundle patch
# 1 个 insert：把 host 插件注册到 DSH profile 根。
# 用户 profile 的 cordis.patch.yml 仍可按 id 覆盖本行的 config。

- insert:
    - id: web-search-plus
      name: '@loommii/dsh-web-search-plus'
      config: {}
